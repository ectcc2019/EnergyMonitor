<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.energymonitor">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.energymonitor.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CadastroActivity"
            android:parentActivityName=".LoginActivity"/>
        <activity android:name=".ResetSenhaActivity"
            android:parentActivityName=".LoginActivity"/>
        <activity android:name=".MeuPerfilActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".GastosActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".MeuConsumoActivity"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".AjustesActivity"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>